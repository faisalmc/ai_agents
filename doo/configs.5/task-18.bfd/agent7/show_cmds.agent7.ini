# --- GENERATED by Agent-7 overlay_planner ---
# Safe to append to Agent-4 run by merging with show_cmds.ini
# Grouped by host and signal (comments only).

# host: B-ASBR-1  platform: cisco-ios-xr  signals: cisco_xr, connection, failed, firewall, hostname, ip, port, tcp

# host: C-ASBR-1  platform: cisco-ios-xr  signals: connection, failed, tcp
