route-policy SID($SID)
set label-index $SID
end-policy
!
router bgp 100
 bgp router-id 1.0.101.9
 address-family ipv4 unicast
  network 1.0.101.9/32 route-policy SID(9)
  allocate-label all
 !
 neighbor-group TO_P
  remote-as 100
  update-source Loopback0
  address-family ipv4 labeled-unicast
  !
 neighbor 1.0.101.7
  use neighbor-group TO_P
  !
 neighbor 1.0.101.8
  use neighbor-group TO_P
